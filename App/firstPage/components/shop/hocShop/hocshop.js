import React,{Component} from 'react';
import {
  compose,
  lifecycle,
  withProps,
  withState
} from 'recompose';

// let addState = withState( 'items', 'setItems', [])

// let addProps = withProps( props => {
//   return props.setItems(props.)
// })


// let addLifeCycle = lifecycle({
//   componentDidMount(){
    
//   }
// })

export default HOCshop = compose(
  // addState,
  // addLifeCycle
)